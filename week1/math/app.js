const {add,subtract,multiply,divide} = require('./math');

const adding = add(200,500);

const sub = subtract(50,20);

const mul = multiply(10,3);

const div = divide(60,2);

console.log(adding,sub,mul,div);
